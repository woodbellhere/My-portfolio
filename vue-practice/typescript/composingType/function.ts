function sum(num1: number, num2: number): number {
  return num + num2;
}

function printPoint(point: { x: number; y: number; z?: number }) {
  console.log(point.x);
  console.log(point.y);
}
export {};
